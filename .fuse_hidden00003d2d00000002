#!/bin/bash

echo "Starting frontend first… 💛"
cd flamix-frontend
pnpm dev &

sleep 2  # wait 2 seconds to let frontend start

echo "Starting backend with sudo now… ⚡"
cd ../backend
sudo pnpm dev

